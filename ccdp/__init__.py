"""Claude Code Display Plugin (working name pending).

Gives every Claude Code session, per project directory, a sandboxed virtual
display it can see and drive the way a human would — pixels out, OS-level input
in — plus a local UI to watch those displays and a tool for Claude to file bugs.

This package is the runtime: surface lifecycle, capture, input, the MCP server,
and the dashboard. See README.md for the full picture.
"""
__version__ = "0.1.13"
PROGRAM_NAME = "Claude Code Display Plugin"
